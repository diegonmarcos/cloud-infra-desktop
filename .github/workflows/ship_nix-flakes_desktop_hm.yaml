# ╔══════════════════════════════════════════════════════════════════╗
# ║                                                                  ║
# ║   GENERATED FILE — DO NOT EDIT                                   ║
# ║                                                                  ║
# ║   Source : 1_workflows/src/cicd/ship_nix-flakes_desktop_hm.yaml
# ║   Engine : 1_workflows/src/scripts/unix-ship-repo-workflow-engine.sh
# ║   Rebuild: ./1_workflows/build.sh
# ║                                                                  ║
# ║   Manual edits will be overwritten on next build.                ║
# ║                                                                  ║
# ╚══════════════════════════════════════════════════════════════════╝

name: "Ship → Desktop home-manager closure (build remotely, never freeze the laptop)"

# WHY: the full Plasma home-manager config is a heavy KDE eval that over-commits
# RAM on the 8GB Surface and can thrash into a freeze (same axis as the NixOS
# system freeze). Build it on a GHA x86 runner; the laptop only IMPORTS +
# activates the prebuilt closure (no eval) via `build.sh pull`.
#
# ALL logic in ba_flakes_desktop/build.sh (verb: ci-build). Thin YAML.
# x86_64 home config -> native x86 runner (no QEMU). Secrets are decrypted at
# activation on the device, so the build needs none.

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "ba_flakes_desktop/src/**"
      - "ba_flakes_desktop/build.sh"
      - "1_workflows/src/cicd/ship_nix-flakes_desktop_hm.yaml"

permissions:
  contents: write        # upload the incremental nix cache as a Release asset
  packages: write        # push the incremental layered HM cache image to GHCR

concurrency:
  group: ship-desktop-hm
  # QUEUE, don't cancel: the closure build takes ~20 min, so cancel-in-progress
  # meant it never completed during active pushes (every new commit killed the
  # in-flight build). `switch` consumes the latest SUCCESSFUL run, so a queued
  # build finishing a commit or two behind HEAD is fine — a completed artifact
  # beats a perpetually-cancelled one.
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      # The HM closure is ~24GB realised; ubuntu-latest ships only ~14GB free on
      # / so the sops-install-secrets Go build hit "no space left on device"
      # (2026-07-16, run 29506574808). Reclaim the ~20-30GB of preinstalled
      # toolchains we never use (Android SDK, .NET, GHC, tool-cache, docker
      # images) before the build. large-packages/swap left off (slow / risky).
      - name: Free up runner disk (large HM closure needs headroom)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          docker-images: true
          swap-storage: false

      - uses: cachix/install-nix-action@v30
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      - name: Build + export home-manager closure (+ push incremental GHCR cache)
        env:
          GHCR_PUSH: "1"                       # build.sh also builds+pushes the layered cache image
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HM_CACHE_IMAGE: ghcr.io/${{ github.repository_owner }}/unix-hm-cache
        run: ./ba_flakes_desktop/build.sh ci-build

      # ── Incremental nix cache → GitHub RELEASE asset (2026-07-11) ──────────
      # Was gh-pages until git rejected the large nar push (HTTP 500 / send-pack
      # disconnect on every run — a git branch can't hold a nix binary cache).
      # Publish ONLY the CUSTOM store paths (closure minus what cache.nixos.org
      # already serves = tens of MB) as a signed nix cache, TAR it, and upload
      # it as one ASSET on a rolling Release tag (--clobber). Release assets
      # hold large binaries fine. The desktop's `build.sh switch` downloads that
      # one asset, extracts, `nix copy --from file://`s the custom paths, and
      # fills the public deps from cache.nixos.org — no 6GB, no 14G, no freeze.
      - name: Publish incremental nix cache as a Release asset
        env:
          NIX_CACHE_PRIVKEY: ${{ secrets.NIX_CACHE_PRIVKEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          CFG=ba_flakes_desktop/src/modules/nix-cache.json
          TAG=$(jq -r .release_tag "$CFG"); ASSET=$(jq -r .asset_name "$CFG")
          TOP="/nix/store/$(cat ba_flakes_desktop/dist-ci/activation.name)"
          echo "toplevel: $TOP  → release $TAG / $ASSET"
          nix path-info -r "$TOP" | sort > /tmp/all.txt
          # CUSTOM paths = closure MINUS what cache.nixos.org already serves.
          # Membership test = probe each path's .narinfo on the cache (the only
          # reliable check; `path-info -r --store <remote>` computes the closure
          # INSIDE the remote and errors because $TOP isn't there → previously
          # marked ALL 4152 paths custom → 6GB asset → over the 2GB cap). Probe
          # in parallel; a path is public iff its narinfo returns 200.
          # ROBUST probe (2026-07-16): the previous -P32/no-retry version marked
          # CACHED paths custom on any transient blip/429 → 474 false-customs =
          # 5.3GB asset (verified: digikam/obs/kdenlive/plasma-workspace all
          # return 200 but were flagged custom). Fix: retries so a blip resolves
          # to its true 200/404, lower parallelism, and treat ONLY a real 200 as
          # public. -f → 404 exits 22 immediately (real custom); --retry handles
          # transient 5xx/429/connection so they never masquerade as custom.
          xargs -a /tmp/all.txt -P 16 -I{} sh -c '
            h=$(basename "{}"); h=${h%%-*}
            curl -s -f -o /dev/null --retry 6 --retry-connrefused \
              --retry-delay 1 --max-time 40 "https://cache.nixos.org/${h}.narinfo" \
              && echo "{}"
          ' | sort > /tmp/public.txt || true
          comm -23 /tmp/all.txt /tmp/public.txt > /tmp/custom.txt
          echo "custom paths: $(wc -l < /tmp/custom.txt) of $(wc -l < /tmp/all.txt) total (rest served by cache.nixos.org)"
          printf '%s' "$NIX_CACHE_PRIVKEY" > /tmp/cache-key.pem
          mkdir -p cache
          xargs -a /tmp/custom.txt -r nix copy --to "file://$PWD/cache?secret-key=/tmp/cache-key.pem"
          cp /tmp/custom.txt "cache/$(jq -r .manifest_file "$CFG")"
          rm -f /tmp/cache-key.pem
          tar -C cache -caf "$ASSET" .
          SZ=$(stat -c%s "$ASSET"); echo "asset size: $((SZ/1024/1024)) MiB"
          # Hard guard: GitHub Release assets cap at 2 GiB. If the delta still
          # exceeds it, the subtraction is wrong or the closure genuinely carries
          # >2GB of non-cached paths — fail LOUDLY (don't blow up in the upload).
          if [ "$SZ" -ge 2147483648 ]; then
            echo "::error::delta asset is $((SZ/1024/1024)) MiB ≥ 2 GiB Release cap — needs chunking or an object-store cache (OCI S3 / R2)."
            exit 1
          fi
          # Rolling release: create the tag once, then clobber the asset each run.
          gh release view "$TAG" -R "$GITHUB_REPOSITORY" >/dev/null 2>&1 \
            || gh release create "$TAG" -R "$GITHUB_REPOSITORY" -t "HM incremental nix cache" \
                 -n "Rolling delta cache for build.sh switch. Auto-updated by CI." --latest=false
          gh release upload "$TAG" -R "$GITHUB_REPOSITORY" "$ASSET" --clobber

      - uses: actions/upload-artifact@v4
        with:
          name: nixos-desktop-hm-closure
          path: ba_flakes_desktop/dist-ci/
          retention-days: 7
          compression-level: 0
