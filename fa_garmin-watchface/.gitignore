# ── Build output ──────────────────────────────────────────────
dist/
result
result-*
*.prg
*.iq

# ── Generated, data-driven artifacts (regenerated by build.sh gen) ──
faces/*/source/gen/
faces/*/resources/drawables/launcher_icon.png

# ── Connect IQ SDK (materialised by flake shellHook, pinned by lock) ──
.ciq-sdk/

# ── Secrets (Pillar 7) — dev signing key never committed ─────────
dist/developer_key.der
dist/developer_key.pem
*.der
*.pem

# ── OS / IDE noise ────────────────────────────────────────────
.DS_Store
*.swp
*~
.vscode/
