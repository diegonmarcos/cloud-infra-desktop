<?xml version="1.0" encoding="utf-8"?>
<!--
  Cloud Keyboard Libs — companion feature-pack APK.

  Bundles libs:media (stickers/GIF), libs:translate (ML Kit), and libs:voice
  (Vosk offline dictation) as a standalone installable so they can be updated
  independently of the keyboard APK. No IME components — this is a launcher
  app only.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- libs:media fetches GIFs from Giphy over HTTPS. libs:media declares NO
         permissions by design — it delegates INTERNET/ACCESS_NETWORK_STATE to
         the consuming app (see libs/media/src/main/AndroidManifest.xml). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- libs:voice records the mic for offline Vosk dictation. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".App"
        android:label="@string/app_name"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FileProvider for libs:media sticker/GIF panel (content:// URIs). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.mediaprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/media_file_paths" />
        </provider>

        <!-- Bundled sticker packs (libs:media BundledStickerProvider). -->
        <provider
            android:name="com.diegonmarcos.superapp.media.BundledStickerProvider"
            android:authorities="${applicationId}.stickercontentprovider"
            android:exported="false"
            android:grantUriPermissions="true" />

    </application>
</manifest>
