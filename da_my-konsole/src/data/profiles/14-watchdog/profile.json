{
  "name": "watchdog",
  "display_name": "Watchdog — System Protection",
  "theme": { "accent": "#c0392b" },
  "sections": [
    {
      "title": "Status",
      "items": [
        { "label": "Status / PSI", "cmd": "watchdog-status" },
        { "label": "🔴 Trigger OOM (Alt+SysRq+F)", "cmd": "watchdog-oom" },
        { "label": "Watch guard logs", "cmd": "watchdog-logs" }
      ]
    },
    {
      "title": "Units",
      "items": [
        { "label": "freeze-guard", "cmd": "journalctl -u freeze-guard.service -f" },
        { "label": "disk-watchdog-v2", "cmd": "journalctl -u disk-watchdog-v2.service -f" },
        { "label": "battery-watchdog", "cmd": "journalctl -u battery-watchdog.service -f" },
        { "label": "systemd-oomd", "cmd": "journalctl -u systemd-oomd.service -f" }
      ]
    },
    {
      "title": "Nix Daemon",
      "items": [
        { "label": "status", "cmd": "systemctl status nix-daemon.service --no-pager | head -30" },
        { "label": "RESET (restart)", "cmd": "sudo systemctl restart nix-daemon.service" }
      ]
    }
  ]
}
