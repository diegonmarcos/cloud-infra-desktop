pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "cloud-unix-termux-boot"

// Data-driven from build.json::modules (FIRE RULE #6: never hardcode lists),
// same loop every constellation repo uses. `include ':app'` was the one
// hardcoded module list left in the fleet.
def buildJson = new groovy.json.JsonSlurper().parse(file('build.json'))
buildJson.modules.each { name, _spec ->
    if (name.startsWith('_')) return  // skip _doc keys
    include ":${name}"
    // Map gradle path :libs:foo -> libs/foo/ (vs. :app -> app/), unless the
    // module declares a `dir` in build.json - that is how a module is SHARED by
    // reference instead of copy-pasted.
    def path = _spec?.dir ?: name.replace(':', '/')
    project(":${name}").projectDir = file(path)
}
