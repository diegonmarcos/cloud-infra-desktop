{
  "_comment": "Multi-voter battery defense (v2). Source of truth for the sysfs-direct battery watchdog. Cross-checks BAT1 + ADP1 + sample-history + wall-clock so a SAM data-path freeze cannot prevent hibernation. ANY enabled voter with critical verdict triggers `systemctl <action_at_critical>` (OR-vote). v1 (single capacity check) failed on 2026-04-30: BAT1 sysfs was wedged at capacity=100 for 3.5h while the laptop drained to firmware power-off — no userspace work was saved. Consumed by `configuration_system-protection-battery.nix`.",
  "version": 2,
  "enabled": true,

  "watch": {
    "_comment": "battery_device + ac_device are sysfs power_supply names. poll_seconds is the systemd OnUnitActiveSec interval. state_dir holds rolling sample history + last-charging timestamp; created by systemd StateDirectory=. history_polls bounds the rolling window (also used by stuck_sam voter).",
    "battery_device": "BAT1",
    "ac_device": "ADP1",
    "poll_seconds": 60,
    "state_dir": "/var/lib/battery-watchdog",
    "history_polls": 10
  },

  "voters": {
    "_comment": "Each voter independently verdicts {ok, low, critical}. Disabled voters are skipped at evaluation time (no fallback). Designed for OR-vote: ANY critical → action.",

    "bat_capacity": {
      "_comment": "Original v1 check. Kernel-derived integer 0..100. Trusts SAM. Fast path; also drives low-warning logging. NOTE: this voter alone failed on 2026-04-30 — kept for early triggering on healthy SAM but no longer the only line of defense.",
      "enabled": true,
      "critical_pct": 5,
      "low_pct": 15,
      "require_status": "Discharging"
    },

    "bat_energy_ratio": {
      "_comment": "Computes ratio from raw energy_now / energy_full µWh. Catches off-by-rounding cases the integer capacity field misses. Same SAM root as capacity, so freezes together — useful only against integer-rounding edge cases, not against full SAM freeze.",
      "enabled": true,
      "critical_pct": 5,
      "require_status": "Discharging"
    },

    "voltage_under_min": {
      "_comment": "DISABLED 2026-05-07 — false positives on Surface Pro 8: voltage_now dips below voltage_min_design under transient load even at 60-90% SoC, triggering a hibernate loop (boot -1 fired at cap=63 / vnow=11.323V / vmin_design ~ 11.4V). Assumption that voltage<voltage_min_design ⇒ empty does not hold for SP8 cells. Other voters (capacity, energy_ratio, alarm, adp_offline_time, stuck_sam) cover the original failure modes without this false-positive vector. Re-enable only with margin_uV calibrated to observed SP8 sag depth + a require_capacity_below floor.",
      "enabled": false,
      "margin_uV": 0,
      "require_status": "Discharging"
    },

    "firmware_alarm": {
      "_comment": "ACPI/SAM alarm field non-zero means firmware itself crossed its low-battery threshold. Should fire well before deep discharge if firmware is healthy.",
      "enabled": true
    },

    "adp_offline_time": {
      "_comment": "Wall-clock fail-safe. ADP1/online=0 (or status=Discharging) for ≥ max_minutes_off_ac since the last observation of {Charging} or {Full + AC online}. Defeats stuck capacity/voltage as long as ADP1 is honest. Trigger window must be tuned to the laptop's battery life on full charge — set below worst-case runtime so it cannot be raced by a real depletion.",
      "enabled": true,
      "max_minutes_off_ac": 240
    },

    "stuck_sam": {
      "_comment": "Catches yesterday's exact failure mode. Triggers when the last min_static_polls samples show {capacity, energy_now, voltage_now} all unchanged AND we observed at least one on-battery indicator (status=Discharging or ac_online=0) within the window. min_static_polls × poll_seconds = grace period before declaring SAM wedged. Conservative: requires both staticness AND an on-battery hint to avoid false positives on a quiescent fully-charged plugged-in machine.",
      "enabled": true,
      "min_static_polls": 5
    }
  },

  "policy": {
    "_comment": "Voters are OR-ed (vote_logic=any). action_at_critical is a systemctl verb. Allowed: hibernate, suspend-then-hibernate, hybrid-sleep, poweroff (suspend verbs are banned by cloud-data-power.json actions.never — Surface S3 is broken). Default `hibernate` requires `resume=` on kernel cmdline and a swap area ≥ /sys/power/image_size on a non-zram backing device.",
    "vote_logic": "any",
    "action_at_critical": "hibernate",
    "action_at_critical_fallback": "poweroff",
    "max_action_failures": 2,
    "_fallback_comment": "2026-06-12 incident: the swapfile was unreachable (p5 superblock destroyed), every `systemctl hibernate` failed with 'Not enough suitable swap space', and the watchdog just retried each poll until the battery hit physical 0% and the firmware power-cut. With this policy the watchdog counts consecutive FAILED critical actions (state file action_failures); after max_action_failures it escalates to action_at_critical_fallback. poweroff loses the RAM session but is a clean filesystem shutdown — strictly better than a firmware power-cut. The counter resets on any poll that is not critical and on any successful action."
  },

  "testers": {
    "_comment": "One invariant per voter input + infra invariants. Wire into reports-logs or a dedicated CI job.",
    "checks": [
      {
        "id": "battery_watchdog_timer_active",
        "description": "battery-watchdog.timer is active",
        "command": "systemctl is-active battery-watchdog.timer",
        "expect": "active"
      },
      {
        "id": "battery_watchdog_service_loaded",
        "description": "battery-watchdog.service is loaded (timer-driven oneshot)",
        "command": "systemctl show -p LoadState --value battery-watchdog.service",
        "expect": "loaded"
      },
      {
        "id": "battery_sysfs_capacity_readable",
        "description": "BAT1/capacity returns an integer 0..100 (voter: bat_capacity)",
        "command": "cat /sys/class/power_supply/BAT1/capacity",
        "expect_regex": "^(100|[0-9]{1,2})$"
      },
      {
        "id": "battery_sysfs_status_readable",
        "description": "BAT1/status returns a known state (voters: bat_capacity, bat_energy_ratio, voltage_under_min, stuck_sam)",
        "command": "cat /sys/class/power_supply/BAT1/status",
        "expect_regex": "^(Charging|Discharging|Full|Not charging|Unknown)$"
      },
      {
        "id": "battery_sysfs_energy_now_readable",
        "description": "BAT1/energy_now returns a non-negative integer µWh (voter: bat_energy_ratio, stuck_sam)",
        "command": "cat /sys/class/power_supply/BAT1/energy_now",
        "expect_regex": "^[0-9]+$"
      },
      {
        "id": "battery_sysfs_energy_full_readable",
        "description": "BAT1/energy_full returns a positive integer µWh (voter: bat_energy_ratio)",
        "command": "cat /sys/class/power_supply/BAT1/energy_full",
        "expect_regex": "^[1-9][0-9]+$"
      },
      {
        "id": "battery_sysfs_voltage_now_readable",
        "description": "BAT1/voltage_now returns a non-negative integer µV (voter: voltage_under_min, stuck_sam)",
        "command": "cat /sys/class/power_supply/BAT1/voltage_now",
        "expect_regex": "^[0-9]+$"
      },
      {
        "id": "battery_sysfs_voltage_min_design_readable",
        "description": "BAT1/voltage_min_design returns a positive integer µV (voter: voltage_under_min)",
        "command": "cat /sys/class/power_supply/BAT1/voltage_min_design",
        "expect_regex": "^[1-9][0-9]+$"
      },
      {
        "id": "battery_sysfs_alarm_readable",
        "description": "BAT1/alarm returns an integer (voter: firmware_alarm)",
        "command": "cat /sys/class/power_supply/BAT1/alarm",
        "expect_regex": "^[0-9]+$"
      },
      {
        "id": "ac_sysfs_online_readable",
        "description": "ADP1/online returns 0 or 1 (voter: adp_offline_time, stuck_sam)",
        "command": "cat /sys/class/power_supply/ADP1/online",
        "expect_regex": "^[01]$"
      },
      {
        "id": "battery_state_dir_exists",
        "description": "StateDirectory created and writable (voters: adp_offline_time, stuck_sam)",
        "command": "test -d /var/lib/battery-watchdog && test -w /var/lib/battery-watchdog && echo ok",
        "expect": "ok"
      },
      {
        "id": "battery_history_file_grows",
        "description": "Rolling sample history file present after first run (voter: stuck_sam)",
        "command": "test -s /var/lib/battery-watchdog/history && echo ok",
        "expect": "ok"
      },
      {
        "id": "resume_param_on_cmdline",
        "description": "Kernel cmdline has resume= so hibernation can resume on next boot",
        "command": "grep -o 'resume=[^ ]*' /proc/cmdline",
        "expect_regex": "^resume=/dev/"
      },
      {
        "id": "loginctl_can_hibernate",
        "description": "logind reports hibernate is permitted (CanHibernate is a logind D-Bus property, NOT a systemctl-show property — `systemctl show -p CanHibernate` silently prints nothing)",
        "command": "busctl call org.freedesktop.login1 /org/freedesktop/login1 org.freedesktop.login1.Manager CanHibernate | awk '{gsub(/\"/,\"\"); print $2}'",
        "expect": "yes"
      },
      {
        "id": "resume_devno_registered",
        "description": "Kernel has a resume device registered (/sys/power/resume != 0:0) — without it hibernate entry fails even with healthy swap (2026-06-12: device appeared after boot, devno never registered, CanHibernate=na)",
        "command": "cat /sys/power/resume",
        "expect_regex": "^([1-9][0-9]*:[0-9]+|0:[1-9][0-9]*)$"
      },
      {
        "id": "disk_swap_active",
        "description": "The non-zram swapfile is active swap (2026-06-12: only zram was active — zram cannot back hibernation)",
        "command": "awk 'NR>1 && $1!~/zram/ {c++} END{print c+0}' /proc/swaps",
        "expect_regex": "^[1-9]$"
      },
      {
        "id": "swap_size_ge_image_size",
        "description": "Disk swap (non-zram) ≥ /sys/power/image_size budget",
        "command": "awk 'NR>1 && $1!~/zram/ {sum+=$3} END{print sum*1024}' /proc/swaps; cat /sys/power/image_size",
        "expect_compare": "first_ge_second"
      }
    ]
  },

  "deployment": {
    "_comment": "Where this config is consumed.",
    "consumers": [
      "~/git/unix/aa_nixos-surface_host/src/modules/configuration_system-protection-battery.nix"
    ],
    "reload_command": "cd ~/git/unix/aa_nixos-surface_host && ./build.sh switch"
  }
}
