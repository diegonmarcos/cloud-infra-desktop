<?xml version="1.0" encoding="utf-8"?>
<!-- Drawer's section pane — vertical list of all sub-pages for the current
     section, mirroring build.json::ui.sections[X].pages[]. Tapping a row
     opens that page in the right content pane (MainActivity.openSectionPage).
     Rows added programmatically into `menu_container`. -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingVertical="4dp">

        <TextView
            android:id="@+id/menu_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="12dp"
            android:paddingTop="8dp"
            android:paddingBottom="4dp"
            android:alpha="0.7"
            android:textAppearance="?attr/textAppearanceLabelSmall" />

        <LinearLayout
            android:id="@+id/menu_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </LinearLayout>
</ScrollView>
