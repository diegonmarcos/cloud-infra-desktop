<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Draw the edge handle over other apps. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Keep the overlay service alive once the user leaves the launcher. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application>
        <!-- Edge overlay: draws the handle + detects swipes. Foreground so it
             survives after the launcher goes to background. -->
        <service
            android:name=".EdgeOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="one_hand_edge_gesture_overlay" />
        </service>

        <!-- Accessibility service: performs the global Back/Home/Recents/etc.
             actions on behalf of the swipe. User enables it in Settings. -->
        <service
            android:name=".OneHandAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/onehand_accessibility" />
        </service>
    </application>
</manifest>
