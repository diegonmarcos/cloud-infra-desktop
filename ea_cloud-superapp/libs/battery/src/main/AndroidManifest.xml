<!--
  ~ SPDX-License-Identifier: Apache-2.0
  ~
  ~ libs:battery — declares the battery components that need a manifest
  ~ entry. PowerStateReceiver fires on charger connect/disconnect to
  ~ checkpoint the energy session. BatterySessionWorker (WorkManager) and
  ~ the dialogs need no manifest entry. Manifest-merged into the app.
  -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".PowerStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
                <action android:name="android.intent.action.ACTION_POWER_DISCONNECTED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
