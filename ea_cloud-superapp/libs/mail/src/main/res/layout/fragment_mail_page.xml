<?xml version="1.0" encoding="utf-8"?>
<!-- Generic placeholder for any Mail page. Title + subtitle from arguments.
     Each FairEmail screen we'll eventually cherry-pick (FragmentMessages,
     FragmentFolders, FragmentOptions*, …) replaces a slot like this. -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="12dp"
        android:paddingVertical="10dp">

        <TextView
            android:id="@+id/page_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceTitleMedium" />

        <TextView
            android:id="@+id/page_upstream"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:alpha="0.6"
            android:fontFamily="monospace"
            android:textAppearance="?attr/textAppearanceLabelSmall" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginVertical="8dp"
            android:background="?attr/colorOutlineVariant" />

        <!-- Children list (Settings sub-pages / More overflow). LinearLayout
             filled programmatically with one row per child. -->
        <LinearLayout
            android:id="@+id/page_children"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </LinearLayout>
</ScrollView>
