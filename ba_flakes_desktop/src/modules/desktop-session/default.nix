# desktop-session/ — session-side guardrails currently scattered as
# system-protection-*.nix at the top of modules/. Will receive them in Phase 2.
# Phase 0 scaffold: empty until leaves are migrated (see PLAN-module-split.md).
{ ... }:
{
  imports = [];
}
