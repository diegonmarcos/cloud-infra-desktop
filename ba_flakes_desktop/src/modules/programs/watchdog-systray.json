{
  "_doc": "Data-driven config for programs/watchdog-systray.nix — a KDE system-tray applet (Wayland-native StatusNotifierItem via python3+AyatanaAppIndicator3) exposing the system-protection stack: live status, a manual OOM trigger, the escape-hatch keybind reminder, and a log viewer. Plus a companion NixOS tray. All labels/commands/units live here; the .nix only wires them. `menu` = ordered list of {label, icon, command} rendered as the SNI's Gtk menu (a trailing Quit entry is always appended by the nix); `default_command` is run on primary (left-click) activate, independent of the menu.",
  "watchdog_tray": {
    "enable": true,
    "icon": "security-high",
    "title": "Watchdog — System Protection",
    "default_command": "bash -lc watchdog-status",
    "menu": [
      { "label": "Status / PSI", "icon": "security-high", "command": "bash -lc watchdog-status" },
      { "label": "🔴 Trigger OOM ({{keybind_primary}})", "icon": "process-stop", "command": "bash -lc watchdog-oom" },
      { "label": "Watch guard logs", "icon": "utilities-log-viewer", "command": "bash -lc watchdog-logs" }
    ],
    "oom": {
      "_comment": "Manual OOM trigger. kernel_sysrq=true uses the in-kernel SysRq OOM killer (echo f > /proc/sysrq-trigger) — the SAME action as the Alt+SysRq+F keybind, works even under heavy load (needs kernel.sysrq bit 64, set via sysctl). Falls back to killing the biggest non-essential RSS hog if sysrq is unavailable.",
      "kernel_sysrq": true,
      "keybind_primary": "Alt+SysRq+F",
      "keybind_convenience": "Meta+Ctrl+K"
    },
    "log_units": ["freeze-guard.service", "disk-watchdog-v2.service", "battery-watchdog.service", "systemd-oomd.service"],
    "status_slices": ["user.slice/user-1000.slice", "workload.slice", "connectivity.slice"]
  },
  "nixos_tray": {
    "enable": true,
    "icon": "nix-snowflake",
    "title": "NixOS",
    "build_sh": "$HOME/git/unix/ba_flakes_desktop/build.sh",
    "default_command": "konsole --hold -e bash -lc '$HOME/git/unix/ba_flakes_desktop/build.sh status'",
    "menu": [
      { "label": "Switch (pull latest, isolated)", "icon": "system-software-update", "command": "konsole --hold -e bash -lc '$HOME/git/unix/ba_flakes_desktop/build.sh switch'" },
      { "label": "Generations", "icon": "document-multiple", "command": "konsole --hold -e bash -lc 'home-manager generations; read'" },
      { "label": "Status", "icon": "dialog-information", "command": "konsole --hold -e bash -lc '$HOME/git/unix/ba_flakes_desktop/build.sh status; read'" }
    ]
  }
}
