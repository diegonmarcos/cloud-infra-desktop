# infra-tools/ — heavy cloud-repo-specific CLIs (wrangler, crawlee, …)
# Toggleable per-host since these can be 1+ GB each.
# Phase 0 scaffold: empty until leaves are migrated (see PLAN-module-split.md).
{ ... }:
{
  imports = [];
}
