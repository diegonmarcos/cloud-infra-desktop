<?xml version="1.0" encoding="utf-8"?>
<!--
  libs:maps — declares the location permissions + foreground-service
  the GPS tracker needs. Manifest-merger folds these into the final
  app manifest at assembly time, so app/AndroidManifest.xml stays
  free of any Maps-specific entries.
  -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application>
        <service
            android:name="com.diegonmarcos.cloudnav.maps.LocationTrackerService"
            android:foregroundServiceType="location"
            android:exported="false" />
    </application>
</manifest>
