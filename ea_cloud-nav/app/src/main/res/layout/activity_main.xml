<?xml version="1.0" encoding="utf-8"?>
<!-- Cloud Nav shell: a content fragment container + bottom navigation.
     There is NO shared search bar — each page (Routes / Navigation / Places)
     renders its OWN search UI overlaid on its map, so the search experience
     is tailored per tab (multi-stop routing vs destination vs simple POI
     lookup). Tabs are data-driven (build.json::ui.nav_tabs) and built in
     MainActivity. Top system-bar inset is applied to `content`. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/nav_surface">

    <FrameLayout
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:labelVisibilityMode="labeled"
        android:background="@color/nav_surface" />
</LinearLayout>
